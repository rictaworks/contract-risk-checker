DATABASE_URL=postgresql://localhost:5432/contract_risk_checker_development
OPENAI_API_KEY=
LANGSMITH_API_KEY=
RAILS_ENV=development
# ローカル開発・CI では必須（32文字以上の乱数）: openssl rand -hex 32
JWT_SECRET=
GOOGLE_CLIENT_ID=
FRONTEND_URL=http://localhost:3000
# ローカル開発のみ: true にすると 127.0.0.1 からのアクセスで認証をバイパスする。
# 本番・CI・共有環境では設定禁止。
DEV_AUTH_BYPASS=
